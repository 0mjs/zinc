---
// Cross-section of a galvanized stack: your code, a thin Zinc coat, net/http steel.
import { Code } from "astro:components";
import zincFrost from "../../themes/zinc-frost.json";

const contracts = [
  { code: "server.Handler = app", note: "Run it on a server you configure" },
  { code: 'app.HandleHTTP("GET /metrics", promhttp.Handler())', note: "Serve a route with any standard handler" },
  { code: "app.UseHTTP(otelhttp.NewMiddleware(\"api\"))", note: "Wrap the app in standard middleware" },
];
---

<section class="coat">
  <div class="copy">
    <p class="kicker">How it fits</p>
    <h2>A thin coat, not a new engine.</h2>
    <p>
      Galvanizing protects steel without replacing it. Zinc does the same for <code>net/http</code>:
      your app is still an <code>http.Handler</code>, and standard handlers, middleware, and servers
      plug straight in.
    </p>
    <ul class="contracts">
      {contracts.map((c) => (
        <li>
          <span class="note">{c.note}</span>
          <div class="snippet"><Code code={c.code} lang="go" theme={zincFrost as any} /></div>
        </li>
      ))}
    </ul>
    <a class="more" href="/guide/http-interoperability/">Zinc and net/http →</a>
  </div>

  <figure class="section-cut" aria-label="Cross-section: your application sits on a thin Zinc layer over net/http">
    <div class="layer app">
      <strong>Your application</strong>
      <span>handlers · services · packages</span>
    </div>
    <div class="layer zn">
      <strong>Zinc</strong>
      <span>routing · binding · errors · middleware</span>
    </div>
    <div class="layer steel">
      <strong>net/http</strong>
      <span>server · request · response writer</span>
    </div>
    <figcaption>
      <span>http.Handler in</span>
      <span>http.Handler out</span>
    </figcaption>
  </figure>
</section>

<style>
  .coat {
    align-items: center;
    display: grid;
    gap: clamp(2.5rem, 6vw, 5rem);
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
  }

  .kicker {
    color: var(--z-muted);
    font: 500 0.72rem/1 var(--sl-font-mono);
    letter-spacing: 0.04em;
    margin: 0 0 1rem;
  }

  h2 {
    color: var(--z-text);
    font: 600 clamp(1.9rem, 3.4vw, 2.6rem) / 1.08 var(--sl-font);
    letter-spacing: -0.035em;
    margin: 0 0 1.1rem;
    text-wrap: balance;
  }

  .copy > p:not(.kicker) {
    color: var(--z-muted);
    font-size: 1rem;
    line-height: 1.65;
    margin: 0 0 1.75rem;
    max-width: 46ch;
  }

  .copy code {
    background: var(--sl-color-bg-inline-code);
    border: 1px solid var(--sl-color-hairline);
    border-radius: var(--z-radius-sm);
    color: var(--z-text);
    font: 400 0.84em var(--sl-font-mono);
    padding: 0.05em 0.35em;
  }

  .contracts {
    display: grid;
    gap: 0.9rem;
    list-style: none;
    margin: 0 0 1.6rem;
    padding: 0;
  }

  .contracts li {
    display: grid;
    gap: 0.4rem;
    margin: 0;
  }

  .note {
    color: var(--z-muted);
    font-size: 0.85rem;
  }

  /* One-line code plates, the same dark finish as every code block. */
  .snippet {
    background: var(--z-plate);
    border: 1px solid #252a31;
    border-radius: 8px;
    overflow: hidden;
  }

  .snippet :global(.astro-code) {
    background: var(--z-plate) !important;
    border: 0;
    border-radius: 0;
    font-size: 0.8rem;
    line-height: 1.5;
    margin: 0;
    overflow-x: auto;
    padding: 0.7rem 0.95rem;
    scrollbar-width: none;
  }

  .more {
    color: var(--z-text);
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
  }

  .more:hover {
    color: var(--z-zn);
  }

  /* The cut: thick app, thin zinc coat, thick steel. */
  .section-cut {
    border: 1px solid var(--sl-color-hairline-light);
    border-radius: 10px;
    margin: 0;
    overflow: hidden;
  }

  .layer {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 1rem;
    justify-content: space-between;
    padding-inline: 1.25rem;
  }

  .layer strong {
    font-size: 0.95rem;
    font-weight: 600;
  }

  .layer span {
    font: 400 0.72rem/1.3 var(--sl-font-mono);
  }

  .app {
    background:
      repeating-linear-gradient(90deg, transparent 0 23px, var(--sl-color-hairline) 23px 24px),
      var(--z-panel);
    color: var(--z-text);
    min-height: 8.5rem;
  }

  .app span {
    color: var(--z-muted);
  }

  .zn {
    background: #c9ced3 url("/zn-spangle.png") center / 180px auto repeat;
    border-block: 1px solid rgba(0, 0, 0, 0.35);
    color: #16191c;
    min-height: 3rem;
  }

  .zn strong {
    font: 400 0.82rem/1 var(--z-font-display);
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .zn span {
    color: #2d3238;
  }

  /* Brushed steel: fine horizontal grain, no highlights. */
  .steel {
    background:
      repeating-linear-gradient(180deg, rgba(255, 255, 255, 0.025) 0 1px, transparent 1px 3px),
      linear-gradient(180deg, #2b3036, #1c2024);
    color: #e2e6ea;
    min-height: 8.5rem;
  }

  .steel span {
    color: #9aa3ad;
  }

  figcaption {
    background: var(--z-panel);
    border-top: 1px solid var(--sl-color-hairline-light);
    color: var(--z-muted);
    display: flex;
    font: 400 0.7rem/1 var(--sl-font-mono);
    justify-content: space-between;
    padding: 0.75rem 1.25rem;
  }

  @media (max-width: 60rem) {
    .coat {
      grid-template-columns: minmax(0, 1fr);
    }
  }
</style>
